import type { BuildProviderStub } from "../contracts.js";

export const viteBuildProviderStub: BuildProviderStub = {
  mode: "vite",
  summary: "Placeholder for a future Vite-backed build provider.",
  notes:
    "Not active. Add a concrete provider implementation and register it in builders/registry.ts when support is ready.",
};
